import React from "react";

export default class Footer extends React.Component {
  render() {
    // var cat = JSON.parse(localStorage.getItem("datalist"));
    // console.log(cat);
    // console.log(cat[0]);
    return (
      <footer>footer</footer>
    );
  }
}